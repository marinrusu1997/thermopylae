import Logger from '@marin/lib.logger';
import { SERVICES } from '@marin/lib.utils';

const logger = Logger.for(SERVICES.AUTH);

// eslint-disable-next-line import/prefer-default-export
export { logger };
