/**
 * Generates the HTML content which needs to be sent in the
 * `Activate Account` email
 *
 * @param {string}		token		Activate account token
 * @param {number}		accountId	Account id
 *
 * @return {string}		HTML content of the email
 */
function getActivateAccountHTML(token, accountId) {
	/** FIXME implement this */
	throw new Error('NOT IMPLEMENTED');
}

// eslint-disable-next-line import/prefer-default-export
export { getActivateAccountHTML };
